const ChatList = ({ onSelectChat }: { onSelectChat: (id: string) => void }) => {

    return (
        <div className="h-screen w-full md:w-1/3 lg:w-1/4  bg-gray-800 text-white border-r">
            ChatList
        </div>
    )
}

export default ChatList